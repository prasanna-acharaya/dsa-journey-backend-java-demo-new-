spring:
  # PostgreSQL Database Configuration for Development
  # PostgreSQL Database Configuration for Development (Neon DB)
  datasource:
    # Neon DB Connection
    url: jdbc:postgresql://ep-mute-poetry-a17sm058-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_einTomHs7b5F
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000
  
  # JPA Configuration for Development
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# Logging Configuration for Development
logging:
  level:
    com.bom.dsa: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# JWT Configuration for Development (use shorter expiration for testing)
jwt:
  expiration: 3600000

# Server Configuration
server:
  port: 8081
